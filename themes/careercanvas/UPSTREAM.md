This folder is an offline placeholder for the CareerCanvas upstream theme.

The V2 project carries the layouts and styles required to build and deploy on its own, so fetching the upstream theme is NOT required for GitHub Pages deployment.

If you want to install/update the official upstream theme later, run `./setup-theme.sh`.
